import { vitePreprocess } from '@sveltejs/vite-plugin-svelte';

export default {
    compilerOptions: {
        // Compiler options go here if any turn out to be needed.
    },
    preprocess: vitePreprocess(),
};
