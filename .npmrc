# pnpm's default, stated rather than assumed: an isolated tree means a package can only
# import what it declares. If somebody "fixes" a missing dependency by hoisting
# everything, an undeclared import stops failing here and starts failing in the browser.
node-linker=isolated
